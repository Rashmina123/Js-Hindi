'use strictmode' //treats code as a newer version

//tc39. EcmaScript Language Grammer

//alert('Hello') // we r using node js not BROWSER.
/* DataTypes Premitive Datatypes
string 
number
boolean
NUll -- representation of Empty. 
Undefine == means not assign any value.
Symbol == for uniq
*/

let a = null
console.log(a)
console.log(typeof(null)) // it gives object
console.log(typeof(undefine)) // it gives undefined
